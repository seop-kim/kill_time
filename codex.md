# 게임 개발 참고사항

이 문서는 이 프로젝트에서 오목을 포함한 멀티플레이 게임을 추가하거나 수정할 때 지켜야 하는 공통 규칙을 기록한다.

## 화면과 레이아웃

- 기준 화면은 FHD 데스크톱이다. 반응형 레이아웃을 추가하지 않는다.
- 창 크기를 줄였을 때 레이아웃이 재배치되거나 세로로 찌그러지지 않고, 고정된 화면 일부가 가려지는 방식으로 유지한다.
- 게임 셀 영역 내부에만 스크롤이 생긴다.
- 하단 시트 탭/상태 표시줄은 FHD 기준 화면 하단에 고정한다.
- 채팅 패널은 전체 화면이 아니라 게임 셀 영역 안에서만 열린다.

## 방과 게임 상태

- 방의 현재 게임 선택은 `rooms/{code}/gameId`에 저장한다. 기존 방에 값이 없으면 `omok`으로 간주한다.
- 게임 탭 변경은 방장만 수행하며, 참여자 화면은 방 구독값을 기준으로 같은 게임 화면으로 전환한다.
- 방 상태는 `waiting`(대기 중), `playing`(게임 중), `finished`(종료) 세 단계로 관리한다.
- 상단 상태 표시는 대기 중에는 `대기 중`, 게임 중에는 `편집 중`으로 표시한다.
- 대기 중 상태 버튼은 방장이 게임을 시작할 수 있을 때 밝은 초록색으로 깜빡인다.
- 게임 시작 전 확인 팝업을 표시한다.
- 오목은 대국자 2명만 게임에 참여한다. 방에는 추가 인원이 옵저버로 입장할 수 있다.
- 대기방의 모든 온라인 참여자는 `대진 참여` 버튼으로 대국 의사를 표시할 수 있다.
- 두 명이 대진 참여 의사를 표시하면 방장 선택 없이 두 명이 자동으로 매치되어 게임이 시작된다.
- 방 소유자와 현재 대국자는 별도 개념이다. 옵저버 두 명이 매치될 수도 있다.
- 참여자 목록은 `게임 중`과 `옵저버` 그룹으로 분리한다.
- 게임에서 나간 사용자 또는 연결 종료 후 15초가 지난 사용자는 참여자 목록에서 제거한다.
- 상대방 연결 종료 후 15초가 지나면 게임을 종료하고 남은 플레이어를 승리 처리한다.
- 게임 시작 시 `gameStartedAt`을 기록한다. 전적의 동일 게임 식별에 사용한다.

## 오목 규칙

- 무르기는 상대방이 둔 바로 전 수 하나를 대상으로 한다.
- 무르기는 상대방의 요청과 수락이 있어야 처리한다.
- 게임 종료 결과는 승리, 패배, 무승부 중 하나로 기록한다.

## 내가 그린 기린 그림 규칙

- 게임 ID는 `girin`이며, 방에 입장한 온라인 참여자 전원이 게임에 참여한다.
- 게임 시작 시 참여자 순서를 무작위로 섞어 `1번`, `2번`, `3번`처럼 번호를 부여한다.
- 번호 순서대로 한 명씩 문제 출제자가 된다.
- 기린 화면도 오목과 같은 행·열 헤더 및 셀 그리드를 사용하고, 상태/그림판/참여자 정보는 필요한 셀을 병합한 영역 안에 배치한다. 별도 게임 제목 카드는 두지 않는다.
- 출제자는 최대 8자의 문제를 입력한 뒤 그림을 그린다. 문제 입력 내용은 출제자 화면에만 표시한다.
- 출제자의 그림은 모든 참여자에게 공유되며, 출제자 1명당 그림 시간은 5분이다.
- 선색은 상단 메뉴의 텍스트 색상 팔레트에서 선택하며, 선택한 색상은 해당 브라우저의 다음 스트로크에 적용한다.
- 선 굵기는 상단 메뉴의 `선 굵기` 선택 상자에서 조정하며, 해당 브라우저의 다음 스트로크에 적용한다.
- 다른 참여자가 채팅에 정답을 정확히 입력하면 해당 정답 입력자가 즉시 승리하고 게임을 종료한다.
- 5분 안에 정답자가 없으면 해당 출제자가 그 퀴즈에서 승리하고 다음 번호의 출제자로 넘어간다.
- 모든 참여자가 한 번씩 출제하면 한 사이클이 끝나며, 승자 없이 게임을 종료한다.
- 문제 입력, 그림판, 5분 타이머, 채팅 정답 판정은 `girinGame` 상태로 오목 상태와 분리해 관리한다.
- 기린 그림 전적은 `totalQuizzes`(총 퀴즈 수), `correctAnswers`(맞춘 수), `stumped`(정답을 못 맞히게 한 출제 성공 수)로 관리한다.

## 사용자 식별과 전적

- 사용자 식별은 IP나 닉네임이 아니라 브라우저별 UUID를 사용한다.
- UUID는 `localStorage`의 `kt_user_id` 키에 한 번 생성해 저장하고, 방이 달라져도 재사용한다.
- 방별 참여 정보는 `kt_room_{방코드}` 키에 저장한다. 기존 저장 정보에 UUID가 없으면 방 입장 시 자동 보완한다.
- Firebase 전적 경로는 다음 구조를 사용한다.

```text
profiles/{userId}/
  nickname
  games/{gameId}/
    played
    wins
    losses
    draws
  recordedGames/{resultId}
```

- 전적은 게임별로 분리한다. 현재 오목의 게임 ID는 `omok`이다.
- 게임 종료 시 해당 게임에 참여한 플레이어만 전적을 기록한다. 옵저버는 기록하지 않는다.
- `recordedGames`로 동일 게임 결과가 새로고침이나 재연결 때문에 중복 집계되지 않도록 한다.
- 전적 저장은 동일 결과에 대해 한 번만 시도하며, 저장 실패가 방 상태 갱신마다 반복 토스트로 표시되지 않도록 한다.
- 프로필 전적은 상단 우측 프로필 아바타를 클릭해 확인한다.
- UUID 방식은 브라우저 로컬 식별자이므로 로그인 기반의 보안 인증이나 부정행위 방지 수단으로 간주하지 않는다.

## 게임 추가 시 지켜야 할 구조

- 게임 탭은 `RoomClient.tsx`의 `GAMES` 목록에 `id`, `label`, `available`을 추가한다.
- 게임별 종료 결과는 공통 전적 함수에 해당 게임 ID를 넘겨 저장한다.
- 게임 세션마다 고유한 시작 시각 또는 세션 ID를 기록해 결과 중복 저장을 막는다.
- 방 상태, 참여자 역할, 승패 결과를 UI 표시와 Firebase 저장에서 동일하게 사용한다.
- 새 게임을 추가해도 프로필 메뉴에는 게임별 전적이 자동으로 나타나도록 `GAMES` 목록을 기준으로 렌더링한다.

## 채팅과 협업 UI

- 채팅은 `추가 기능` 메뉴가 아니라 코파일럿 아이콘으로 열고 닫는다.
- 코파일럿 아이콘은 `public/copilot-icon.png` 이미지를 그대로 사용한다.
- 채팅에는 엔터 전송과 보내기 버튼을 모두 제공한다.
- 채팅창을 닫았다 다시 열면 항상 마지막 메시지 위치로 연다.
- 기본 채팅 글자 크기는 8px이며, 사용자가 1px 이상으로 직접 입력할 수 있다.
- 채팅창 너비는 입력 필드가 아니라 왼쪽 경계를 마우스로 드래그해 조절한다.
- 사용자별 채팅 색상은 안정적인 UUID를 기준으로 정한다.
- 공유 버튼은 즉시 복사하지 않고 문서 코드를 보여주는 드롭다운과 `복사하기` 버튼을 표시한다.

## 변경 후 확인

기능을 추가하거나 수정한 뒤 다음을 실행한다.

```text
npm test -- --run
npm run lint
npm run build
git diff --check
```
